version: 0.2

phases:
  install:
    commands:
      - npm install
      - npm install -g newman
  build:
    commands:
      - newman run NewmanStack.template.json
      # - npm run build 
      # - npm run test 
      # - npm run cdk -- synth

artifacts:
  base-directory: cdk.out
  files:
    - '*/'